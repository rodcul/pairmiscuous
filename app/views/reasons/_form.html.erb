<%= form_for(@reason) do |f| %>
  <% if @reason.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@reason.errors.count, "error") %> prohibited this reason from being saved:</h2>

      <ul>
      <% @reason.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= hidden_field(:user_id, :token, :user_id => 22)%>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= current_user.name %>
  </div>
  <div class="field">
    <%= f.label :pair_id %><br>
    <!-- <%= f.number_field :pair_id %> -->
    # PAIR #
  </div>
  <div class="field">
    <%= f.label :reason %><br>
    <%= f.text_field :reason %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
